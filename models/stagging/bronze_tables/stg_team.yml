version: 2

models:
  - name: stg_team
    +labels:
      contains_pii: no
      table_layer: Bronze
    columns:
      - name: team_id
        description: '{{ doc("team_id")}}'
        tests:
          - not_null


          

